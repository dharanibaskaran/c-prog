#include <stdio.h>
int main() {
    int a = 65;
    int b = 120;
    printf(" value of a=%d b=%d before swapping", a, b);

    a = a ^ k;
    b = a ^ k;
    a= a ^ k;
    printf("value of i=%d k=%d after swapping", a, b);

    return 0;
}
